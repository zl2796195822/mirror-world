POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mirror
POSTGRES_USER=mirror
POSTGRES_PASSWORD=mirror_dev_only
DATABASE_URL=postgres://mirror:mirror_dev_only@localhost:5432/mirror
REDIS_URL=redis://localhost:6379
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=mirror_minio
MINIO_SECRET_KEY=mirror_minio_dev_only
# API skeleton defaults; keep the server bound to loopback unless deployment explicitly requires otherwise.
HOST=127.0.0.1
PORT=3001
# Only enable the explicit seed identity in local Next.js development.
MIRROR_DEV_AUTH=true
