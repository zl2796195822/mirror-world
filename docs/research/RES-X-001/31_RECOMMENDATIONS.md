# 31 Recommendations

These are future recommendations only; none is an implementation instruction for this task.

1. Treat `origin/main@2e526d3` PRE-AL-07 as the current M3 v1 scheduler boundary: one bounded driver, durable/rebuildable due sources, completion before decision wake, Kernel-only commits.
2. Before any M7/M8/M9/M10 port, run one Compatibility Review against latest main and re-audit all stale references; do not inherit frozen pending labels as current facts.
3. Freeze separate W/I/V namespaces and a legal combination matrix, then publish old M5 value mappings as a migration note rather than changing M5 in place.
4. Make M9’s orthogonal identity model the candidate formal port: ResidentId, origin, control, cognition, embodiment and ActorRef remain separate; require security/privacy review.
5. Extend the current driver contract only through a single owner; domain packages may produce typed due/wake inputs but may not run resident, cognition or economy schedulers of their own.
6. Define one catch-up rule: no live Provider call for historical replay; existing frozen envelope or deterministic fallback only, with explicit missing-envelope/defer behavior.
7. Define projection/realtime cursors and write gating separately from World Time/Event Ledger sequence; snapshot recovery must be observable and world-scoped.
8. Prove attention bonus, Provider quota, Proxy Charter and cross-world isolation with negative tests before treating any visual/user signal as cognition policy input.
