# 22 Rerun Plan

Run-08 remains the immutable failed baseline. No rerun is performed by this review.

## Order of operations

1. Approve and freeze the exact Hard Gate #3 clarification and artifact schema.
2. Run targeted tests on clean PostgreSQL: positive and zero-food EAT; bounded unavailable resource path; bootstrap-wide WORK wakes; 10/15-minute commute arrival before 09:00; exact WORK start and return; TALK paired accounting and negative funnel; existing ADR, replay, determinism, isolation, liveness, and no-BUY/no-LLM checks.
3. Review targeted evidence. If TALK rows show legal opportunities with suppressed candidates, authorize the smallest policy fix; if they show no opportunities, retain `INSUFFICIENT_EVIDENCE`/valid bounded unavailability and do not alter topology.
4. Execute a new full 30-resident × 43,200-World-Minute run with a new run ID and lineage pointing to `20260910-run-08`. Do not reuse or overwrite run-08.
5. Re-run all 15 hard gates and the final status review. A conditionalized Gate #3 must still fail for broad eligible response gaps such as unresolved WORK 4/26; it is not a pass-by-count gate.

| Requirement                     | Decision                                           |
| ------------------------------- | -------------------------------------------------- |
| targeted verification           | required before full Gate                          |
| full 30×30 rerun                | required after targeted pass and governance freeze |
| new run ID                      | required                                           |
| run-08 preservation             | required                                           |
| M3-T05 execution in this review | prohibited; remains defined/not started            |
