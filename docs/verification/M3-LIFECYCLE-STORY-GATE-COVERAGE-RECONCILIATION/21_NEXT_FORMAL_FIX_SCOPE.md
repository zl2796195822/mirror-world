# 21 Next Formal Fix Scope

## Proposed task

`M3-LIFECYCLE-STORY-GATE-COVERAGE-FIX` — one independent formal fix task, to be registered and authorized after this review. It is intentionally bounded; it is not executed here.

### In scope

- Freeze the approved Hard Gate #3 eligibility/necessity and paired-contact accounting clarification, including negative-funnel artifact fields.
- Integrate the existing scheduler's bootstrap-wide WORK boundary/pre-shift preparation path with deterministic commute slack; preserve exact 09:00 authorization and existing `m3-scheduler-v2`.
- Persist bounded EAT unavailable-resource evidence and TALK opportunity/candidate negative evidence; then inspect social priority only where the new evidence identifies a real opportunity drop.
- Add targeted clean-PostgreSQL verification for EAT feasibility, WORK wake/commute timing, TALK accounting, replay, determinism, isolation, zero LLM, and zero BUY settlement.

### Out of scope

BUY/economy, fixture food grants, random MOVE, forced TALK, late WORK grace, new scheduler, rewrites of ADR-0011/0012, M4/M5/M6, and the full 30×30 rerun itself.

| Decision                                     | Result                                                                          |
| -------------------------------------------- | ------------------------------------------------------------------------------- |
| formal fix task count                        | 1                                                                               |
| production code change expected in next task | yes, scheduler integration and evidence instrumentation                         |
| policy change expected                       | WORK wake consumption; TALK policy only if targeted evidence proves it          |
| fixture change expected                      | no                                                                              |
| Gate spec change expected                    | yes, Hard Gate #3 only                                                          |
| new domain ADR                               | no current evidence; governance approval for the frozen Gate change is required |
