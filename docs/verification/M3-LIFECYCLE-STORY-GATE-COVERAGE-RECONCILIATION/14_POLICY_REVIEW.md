# 14 Policy Review

| Action | Policy finding                                                                                                    | Required decision                                                                        |
| ------ | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| EAT    | Positive-resource path executes; zero-food residents have no legal M3 consumable opportunity                      | no EAT policy workaround; use conditional Gate contract and bounded unavailable evidence |
| WORK   | Work obligation is recognized at the exact boundary, but commute preparation is too late for a 10/15-minute route | consume a deterministic pre-shift wake; preserve exact 09:00 authorization               |
| TALK   | Paired contacts are legal and deterministic; six no-contact rows lack opportunity/candidate negatives             | instrument first, then review candidate priority; no policy verdict yet                  |
| MOVE   | Four no-MOVE residents are unemployed with no formal MOVE necessity                                               | no movement policy change                                                                |

Policy changes must preserve `Observation → Need → Goal → Candidate → Rule Decision → ActionRequest → Kernel`. The allowed next change is a causal wake/opportunity correction, not threshold tuning, forced scripts, or a special Gate resource grant.

### Classification boundary

`POLICY_COVERAGE_MISMATCH` is confirmed for the all-resident Gate expectation and is secondary to the WORK scheduler gap. `DECISION_PRIORITY_DEFECT` and `TOPOLOGY_FEASIBILITY_DEFECT` are not proven for TALK because the negative funnel was not stored. No Kernel `IMPLEMENTATION_DEFECT` is proven.
