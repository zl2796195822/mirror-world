# 27 Pending Decisions and Registration Gates

The behavior itself is frozen. The following are formal governance gates, not
open behavioral alternatives:

1. Register and accept `ADR-M3-EAT-KERNEL-CONSUMABLE-CAPABILITY`, preserving
   Kernel/Economy ownership and the event-backed bounded food delta.
2. Register and accept `ADR-M3-TALK-PAIRED-RUNTIME-LOCK`, preserving the single
   initiator request and atomic two-row lock.
3. Register the proposed implementation task name without inventing a
   permanent task number in this reconciliation.

If an ADR owner rejects the frozen recommendation, the implementation task is
blocked and this package must be revised through a new explicit
reconciliation. No implementer may substitute an in-memory resource, unlocked
TALK, multi-actor request, or Life-owned mutation.

There are no remaining choices about BUY ownership, payroll exclusion,
dialogue/Memory/Relationship exclusion, scheduler ownership, World-Time
durations, event names, or Story Sanity Hard Gates.
